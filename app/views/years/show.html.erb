<div class="content">
  <h1>Year: <%= @project_year.date %></h1>
  <p>Belongs to project: <%= @project_year.project.name %>

  <div><%= link_to 'Back', years_path %></div>
  <div><%= link_to 'Edit year', edit_year_path %></div>
  <div><%= link_to 'Delete year', year_path(@project_year),
                method: :delete,
                data: { confirm: 'Are you sure?' } %></div>
</div>
