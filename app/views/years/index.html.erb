<div class="content">
  <% if @project_years.size == 0 %>
    <p>No years exist.</p>
  <% else %>
    <h1>Years</h1>
    <% @project_years.each do |year| %>
      <div><%= link_to year.date, year %></div>
    <% end %>
  <% end %>

  <%= link_to 'Create new year', new_year_path %>
</div>
